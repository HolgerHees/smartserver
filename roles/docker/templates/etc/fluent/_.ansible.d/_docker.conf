<filter docker.*>
  @type record_modifier
  <record>
    group _${tag_parts[1]}
    level UNKNOWN
    container_log ${record['log']}
    container_source ${record['source']}
  </record>
  remove_keys log,source
</filter>
