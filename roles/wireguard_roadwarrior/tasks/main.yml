- name: set version
  set_fact:
    alpine_version: "3.11.6"
  tags: [ 'update_notifier_configs' ]
  
- name: prepare config folder
  file:
    path: "{{global_etc}}wireguard_roadwarrior"
    state: directory
    owner: "root"
    group: "root"
    mode: 750

- name: check started docker
  systemd:
    name: docker
    state: started

- name: check docker image exists
  shell: "docker images custom_wireguard_roadwarrior:{{alpine_version}}"
  register: image_exists
  changed_when: "'custom_wireguard_roadwarrior' not in image_exists.stdout"
  
- name: prepare build folder
  file:
    path: "{{global_build}}wireguard_roadwarrior/"
    state: directory
    owner: root
    group: root
    mode: 0750
  when: "'custom_wireguard_roadwarrior' not in image_exists.stdout"
  
- name: copy dockerfiles
  template:
    src: "roles/wireguard_roadwarrior/templates/container/{{item}}"
    dest: "{{global_build}}wireguard_roadwarrior/{{item}}"
    owner: root
    group: root
  with_items:
    - Dockerfile
    - wg-startup.sh
  when: "'custom_wireguard_roadwarrior' not in image_exists.stdout"
  
# async docker build is used to show progress and prevent travisci killing our job because of inactivity
- name: compose docker image => image does not exists
  shell: "docker build -t custom_wireguard_roadwarrior:{{alpine_version}} {{global_build}}wireguard_roadwarrior/ --build-arg VERSION={{alpine_version}}"
  async: 600
  poll: 0
  register: docker_build
  when: "'custom_wireguard_roadwarrior' not in image_exists.stdout"

- name: wait for docker image => image does not exists
  async_status: jid={{ docker_build.ansible_job_id }}
  register: job_result
  until: job_result.finished
  retries: 10
  delay: 60
  when: "'custom_wireguard_roadwarrior' not in image_exists.stdout"
  
- name: create docker container
  docker_container:
    name: wireguard_roadwarrior
    image: "custom_wireguard_roadwarrior:{{alpine_version}}"
    state: present
#    network_mode: "none"
#    recreate: true
    env:
      TZ: "{{timezone}}"
      DNS_SERVER: "{{server_ip}}"
      SERVER_NETWORK: "{{server_network}}"
      VPN_NETWORK: "{{mobile_vpn_network}}"
      ENDPOINT: "{{public_server_domain}}"
      PORT: "{{vault_vpn_roadwarrior_public_port}}"
    log_driver: journald
    log_options:
      tag: custom_wireguard_roadwarrior
    capabilities:
      - NET_ADMIN
      - SYS_MODULE
    networks:
      - name: "hostaccess"
    networks_cli_compatible: yes
    volumes:
      - '{{global_etc}}wireguard_roadwarrior:/etc/wireguard/config:z'
    ports:
     - "{{vault_vpn_roadwarrior_internal_port}}:51820/udp"
  notify: "restart wireguard_roadwarrior"

#- name: clean old docker images => image does not exists
#  shell: "docker rmi -f $(docker images --filter \"dangling=true\" -q)"
#  when: "'custom_wireguard_roadwarrior' not in image_exists.stdout"
  
# ***** FINALIZE *****
- name: create systemd service
  vars:
    container_name: "wireguard_roadwarrior"
  import_tasks: roles/container/tasks/add_docker_service.yml

- name: register update notifier
  vars:
    name: "wireguard_roadwarrior"
    type: "docker"
    url: "https://alpinelinux.org/"
    config: {
      repository: "alpine",
      pattern: "^([0-9]{1,2}\\.[0-9\\.]+)$" 
    }
  import_tasks: roles/update_notifier/tasks/add_notifier.yml

- name: trigger service check handler
  vars:
    notify: "restart wireguard_roadwarrior"
    service: "wireguard_roadwarrior.service"
  import_tasks: roles/base/tasks/service_check.yml
