apiVersion: 1

#deleteDatasources:
#  - name: Prometheus
#    orgId: 1

datasources:
  - name: Prometheus
    uid: "prometheus"
    type: prometheus
    typeLogoUrl:
    access: proxy
    orgId: 1
    url: http://prometheus:9090
    jsonData:
      "httpMethod": "POST"
    readOnly: true
