- name: "restart wireguard_cloud"
  systemd:
    name: "wireguard_{{vault_cloud_vpn_networks[main_network].interface.name}}"
    state: restarted
    daemon_reload: yes
    enabled: yes
    masked: no
  listen: "restart wireguard_cloud"
