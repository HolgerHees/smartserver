- name: set vcontrold version
  set_fact:
    vcontrold_version: 'v0.98.9'
    debian_version: 'buster-20200514-slim'
  tags: [ 'update_notifier_configs' ]
  
- name: prepare needed folder
  file:
    path: "{{item}}"
    state: directory
    owner: root
    group: root
    mode: 0750
  with_items:
    - "{{global_etc}}vcontrold/"
    - "{{global_log}}vcontrold/"
    - "/opt/heating_control"
    - "/opt/heating_sensors"

- name: get gid from "dialout"
  shell: "getent group dialout | cut -d: -f3"
  register: group_dialout_id
  changed_when: no

- name: build docker image
  vars:
    name: "vcontrold"
    image_name: "custom_vcontrold"
    image_version: "{{debian_version}}-{{vcontrold_version}}"
    files:
      - roles/vcontrold/templates/container/Dockerfile
  import_tasks: roles/container/tasks/build_docker_image.yml

- name: connect vcontrold with mosquitto
  docker_network:
    name: vcontrold_databases
    connected:
      - mosquitto
    appends: yes
  tags: ['mosquitto']

- name: create docker container
  docker_container:
    name: vcontrold
    image: "custom_vcontrold:{{debian_version}}-{{vcontrold_version}}"
    state: present
#    recreate: true
    env:
      TZ: "{{timezone}}"
    log_driver: journald
    log_options:
      tag: vcontrold
    networks:
      - name: "vcontrold_databases"
    networks_cli_compatible: yes
    volumes:
      - '{{global_etc}}vcontrold/:/etc/vcontrold:ro,z'
      - '{{global_log}}vcontrold/:{{global_log}}vcontrold:z'
      - '/opt/heating_control/:/opt/heating_control:ro,z'
    #  - '/dataDisk/build/vcontrold_container/:/vcontrold:z'
    devices:
      - "/dev/ttyOpenHabHeizung:/dev/ttyOpenHabHeizung:rwm"
    ports:
      - "127.0.0.1:3002:3002/tcp"
  notify: "restart vcontrold"

- name: set logrotate job
  vars:
    logrotate_path: "{{global_log}}vcontrold/*.log"
  copy:
    content: "{{ lookup('template', 'templates/logrotate') }}"
    dest: "/etc/logrotate.d/ansible_vcontrol"
    owner: root
    group: root
    mode: 0640

- name: copy config and service
  template:
    src: "templates{{item.src}}"
    dest: "{{item.dest}}"
    owner: root
    group: root
    mode: 0640
  with_items:
    - { src: "/etc/vcontrold/vcontrold.xml", dest: "{{global_etc}}vcontrold/vcontrold.xml" }
    - { src: "/etc/vcontrold/vito.xml", dest: "{{global_etc}}vcontrold/vito.xml" }
    - { src: "/etc/udev/rules.d/10-vcontrold.rules", dest: "/etc/udev/rules.d/10-vcontrold.rules" }
  notify: "restart vcontrold"

- name: copy cmd templates
  template:
    src: "templates/opt/{{item}}"
    dest: "/opt/{{item}}"
    owner: root
    group: root
    mode: 0640
  with_items:
    - "heating_control/heizung.cmd"
    - "heating_control/heizung.tpl"

- name: copy cmd's
  template:
    src: "templates/opt/{{item}}"
    dest: "/opt/{{item}}"
    owner: root
    group: root
    mode: 0750
  with_items:
    - "heating_control/heizung.bash"
    - "heating_control/heizungBetriebsart.bash"
    - "heating_sensors/sensoren.bash"
  
- name: copy cmd binaries
  copy:
    src: "templates/opt/{{item}}"
    dest: "/opt/{{item}}"
    owner: root
    group: root
    mode: 0750
  with_items:
    - "heating_sensors/TempCmd"

- name: create cron job => is_production
  vars:
    name: "Heating Control"
    cmd: "/usr/bin/flock -n {{global_tmp}}heizung.lock /opt/heating_control/heizung.bash"
    file: "ansible_vcontrold"
    minute: "*/1"
  import_tasks: roles/cron/tasks/add_cronjob.yml
  when: "is_production|bool"

- name: create cron job => is_production
  vars:
    name: "Heating Sensors"
    cmd: "/opt/heating_sensors/sensoren.bash"
    file: "ansible_vcontrold"
    minute: "*/1"
  import_tasks: roles/cron/tasks/add_cronjob.yml
  when: "is_production|bool"

- name: create cron job
  vars:
    name: "Heating Cleanup"
    cmd: "find {{global_log}}heizung/ -mtime +14 -exec rm {} +"
    file: "ansible_vcontrold"
    hour: "0"
    minute: "50"
  import_tasks: roles/cron/tasks/add_cronjob.yml

# ***** FINALIZE *****
- name: create systemd service
  vars:
    container_name: "vcontrold"
    trigger_name: "vcontrold"
  import_tasks: roles/container/tasks/add_docker_service.yml

- name: register service watcher
  vars:
    name: "vcontrold"
  import_tasks: roles/systemd_watcher/tasks/add_watcher.yml

- name: register update notifier
  vars:
    name: "vcontrold"
    type: "github"
    url: "https://github.com/openv/vcontrold/releases"
    config: { 
      project: "openv/vcontrold", 
      pattern: "^v([0-9\\.]+)$",
      version: "{{vcontrold_version}}" 
    }
  import_tasks: roles/update_notifier/tasks/add_notifier.yml

- name: register update notifier
  vars:
    name: "debian"
    type: "docker"
    url: "https://debian.org/"
    config: {
      repository: "debian",
      pattern: "^buster-([0-9]+)-slim$" 
    }
  import_tasks: roles/update_notifier/tasks/add_notifier.yml

- name: trigger vcontrold handler
  vars:
    notify: "restart vcontrold"
    service: "vcontrold.service"
  import_tasks: roles/base/tasks/service_check.yml
