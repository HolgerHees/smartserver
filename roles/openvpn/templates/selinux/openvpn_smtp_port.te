
module openvpn_smtp_port 1.0;

require {
	type openvpn_t;
	type smtp_port_t;
	class tcp_socket name_bind;
}

#============= openvpn_t ==============

#!!!! This avc can be allowed using the boolean 'nis_enabled'
allow openvpn_t smtp_port_t:tcp_socket name_bind;
