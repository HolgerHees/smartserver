- name: restart apache
  systemd:
    name: "{{ 'apache2' if ansible_distribution == 'openSUSE Leap' else 'httpd' }}"
    state: restarted
    daemon_reload: yes
    enabled: yes
    masked: no
  listen: "restart apache"
