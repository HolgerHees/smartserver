- name: copy apache config
  template:
    src: "{{config_file}}"
    dest: "{{www_etc}}_.ansible.d/{{config_file | basename}}"
    owner: root
    group: root
    mode: 0644
  notify: "restart apache"
  tags: ['apache_conf']

