TIMEZONE="{{timezone}}" 
HOSTNAME="{{item.name | lower}}"
IS_AP={{ '1' if 'ap' in item.config.roles else '0'}}
ADDITIONAL_PACKAGES="{{ item.config.packages if item.config.packages is defined else ''}}"

