TIMEZONE="{{timezone}}" 
HOSTNAME="{{hostname}}"
IS_AP={{is_ap}}
INSTALL_PACKAGES="{{install_packages | unique | join(' ') }}"
REMOVE_PACKAGES="{{remove_packages | unique | join(' ') }}"
ENABLED_SERVICES="{{enabled_services | join(' ') }}"
