{
    "tag": "{{tag}}"
{% if packages is defined %}
    ,"packages": [ "{{packages | join("\",\"") }}" ]
{% endif %}
{% if roles is defined %}
    ,"roles": [ "{{roles | join("\",\"") }}" ]
{% endif %}
}
