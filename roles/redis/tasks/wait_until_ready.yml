- name: check redis is running
  systemd:
    name: redis
    state: started
    daemon_reload: yes
  changed_when: no

- name: wait for redis to become ready
  wait_for:
    path: "{{redis_socket_path}}redis.sock"
