- name: restart nfs-server
  systemd:
    name: "{{ 'nfsserver' if ansible_distribution == 'openSUSE Leap' else 'nfs-server' }}"
    state: restarted
    daemon_reload: yes
    enabled: yes
    masked: no
  listen: "restart nfs-server"
