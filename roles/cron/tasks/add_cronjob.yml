- name: "create cron job '{{title}}'"
  vars:
    cron_name: "{{title}}"
    cron_cmd: "{{cmd}}"
  cron:
    name: "{{cron_name}}"
    job: "{{ lookup('template', 'templates/cron_job') }}"
    cron_file: "{{file}}"
    user: "{{user | default ('root')}}"
    weekday: "{{weekday | default ('*')}}" 
    hour: "{{hour | default ('*')}}"
    minute: "{{minute | default ('*')}}"
    state: present
