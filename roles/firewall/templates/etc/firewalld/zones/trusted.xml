<?xml version="1.0" encoding="utf-8"?>
<zone target="ACCEPT">
  <short>Trusted</short>
  <description>All network connections are accepted.</description>
  <source address="{{server_network}}/24"/> <!-- internal -->
  <source address="{{docker_hostaccess_network}}/24"/> <!-- docker host access -->
{% if not ansible_remote_ip.startswith(server_subnet) %}  <source address="{{ansible_remote_ip}}"/> <!-- ansible -->
{% endif %}
{% if vpn_mobile_network is defined %}  <source address="{{vpn_mobile_network}}/24"/> <!-- mobile vpn -->
{% endif %}
  <masquerade/>
</zone>
